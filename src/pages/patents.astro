---
import Base from '../layouts/Base.astro'
import patents from '../../public-data/patents.json'
import software from '../../public-data/software.json'
import standards from '../../public-data/standards.json'
---
<Base title="专利" description="王岳的专利、软件著作权和标准">
  <style>
    .page-header { margin-bottom: 32px }
    .page-header h1 { font-size: 26px; font-weight: 800; margin-bottom: 6px }
    .page-header p { color: var(--gray); font-size: 14px }
    h2 { font-size: 18px; font-weight: 700; margin: 32px 0 14px; padding-bottom: 8px; border-bottom: 2px solid var(--border) }
    .card-list { display: flex; flex-direction: column; gap: 10px }
    .card {
      background: #fff; border: 1px solid var(--border); border-radius: 8px;
      padding: 14px 16px;
    }
    .card-title { font-size: 14px; font-weight: 600; margin-bottom: 4px; line-height: 1.5 }
    .card-meta { font-size: 12px; color: var(--gray) }
    .badge {
      display: inline-block; padding: 1px 7px; border-radius: 4px;
      font-size: 11px; font-weight: 600; margin-right: 6px;
    }
    .badge-purple { background: #ede9fe; color: #5b21b6 }
    .badge-teal { background: #ccfbf1; color: #065f46 }
  </style>

  <div class="page-header">
    <h1>专利 / 软著 / 标准</h1>
    <p>{patents.length} 项专利 · {software.length} 项软件著作权 · {standards.length} 项标准</p>
  </div>

  <h2>专利（{patents.length}）</h2>
  <div class="card-list">
    {patents.map((p: any) => (
      <div class="card">
        <div class="card-title">
          {p.title}
          {p.pdf_url && <a href={p.pdf_url} target="_blank" rel="noopener" class="badge badge-teal" style="text-decoration:none;margin-left:8px">PDF</a>}
        </div>
        <div class="card-meta">
          <span class="badge badge-purple">{p.patent_type || '专利'}</span>
          {p.patent_number && <span>{p.patent_number} · </span>}
          {p.inventors && p.inventors.slice(0, 6).join(', ')}
          {p.inventors && p.inventors.length > 6 ? ', et al.' : ''}
          {p.grant_date && <span> · 授权日期 {p.grant_date}</span>}
          {!p.grant_date && p.application_date && <span> · 申请日期 {p.application_date}</span>}
        </div>
      </div>
    ))}
  </div>

  <h2>软件著作权（{software.length}）</h2>
  <div class="card-list">
    {software.map((s: any) => (
      <div class="card">
        <div class="card-title">
          {s.title}
          {s.pdf_url && <a href={s.pdf_url} target="_blank" rel="noopener" class="badge badge-teal" style="text-decoration:none;margin-left:8px">PDF</a>}
        </div>
        <div class="card-meta">
          <span class="badge badge-teal">软著</span>
          {s.registration_number && <span>{s.registration_number} · </span>}
          {s.authors && s.authors.slice(0, 6).join(', ')}
          {s.date && <span> · {s.date.slice(0, 4)}</span>}
        </div>
      </div>
    ))}
  </div>

  {standards.length > 0 && (
    <>
      <h2>标准（{standards.length}）</h2>
      <div class="card-list">
        {standards.map((s: any) => (
          <div class="card">
            <div class="card-title">{s.title}</div>
            <div class="card-meta">
              {s.standard_number && <span>{s.standard_number} · </span>}
              {s.authors && s.authors.slice(0, 6).join(', ')}
            </div>
          </div>
        ))}
      </div>
    </>
  )}
</Base>
